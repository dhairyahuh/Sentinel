# Sentinel Environment Configuration (Optional)
# The pipeline and demo console work completely offline without any API keys or network access.
# If refreshing LLM-generated narrative caches:
OPENAI_API_KEY=
REDTEAM_LLM_BASE_URL=https://api.openai.com/v1

# Frontend environment mode (optional: 'demo' or 'live')
VITE_MODE=demo
